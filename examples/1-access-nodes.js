// NAVIGATE DOM -------------------------
someNode.children; // select children
someNode.firstElementChild; // select first child
someNode.nextElementSibling; // select next sibling
someNode.parentElement; // select parent

// SEARCH DOM ------------------------------
// select 1 node by its id
document.getElementById("my-id-");
// select first node that matches selector
document.querySelector("my-selector");
// select all nodes that match selector
document.querySelectorAll("my-selector");
